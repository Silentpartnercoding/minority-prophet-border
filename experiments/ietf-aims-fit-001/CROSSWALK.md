# AIMS-FIT-001 mechanical crosswalk

The evaluated AIMS text is `draft-klrc-aiagent-auth-03`, SHA-256
`41b3899039a6c978c69eed32b17485a72f6dfef72c96d38eec9bbe9b0190e37a`.
The versions of WPT, Transaction Tokens, and cross-domain identity chaining are
the versions cited by AIMS v03, not later editor revisions.

| Required semantic | Existing Border evidence | AIMS / pinned primitive | Fit | Consequence |
|---|---|---|---|---|
| **MANDATE: A is authenticated and key-bound** | `authorized_invocation.md`; `mandate-v1.json`; Mandate binds requester identity and key thumbprint | AIMS §§6-7; §9.2.1; WPT-01 `wth`; RFC 9068 `client_id` | **EXACT** | A can be represented as a cryptographically authenticated agent workload. |
| **MANDATE: A is authorized to request the exact action** | `mandate-v1.json` cases `requester_not_authorized`, `wrong_page`, `changed_payload` | AIMS §10.3 permits scopes and additional authorization attributes; Txn-Tokens-09 §9.2.3 `tctx` can contain immutable authorization details | **AMBIGUOUS** | `tctx` can carry action data, but AIMS defines no portable `may-request` semantic or verifier rule for it. |
| **MANDATE: B is authenticated and key-bound** | `authorized_invocation.md`; executor credential and key-substitution vectors | AIMS §§6-7; §9.2.1; WPT-01 `wth`; RFC 9068 `client_id` | **EXACT** | B can be represented as a distinct authenticated agent workload. |
| **MANDATE: B has its own authority to execute the exact action** | `mandate-v1.json` cases `executor_not_authorized`, executor subject/key substitutions | AIMS §10.4.2 allows an Agent to obtain authorization on its own behalf; §10.3 access-token `scope` and authorization attributes; Txn-Tokens-09 `tctx` | **AMBIGUOUS** | B's independent token is representable, but exact action semantics remain profile-defined. |
| **MANDATE: A's signed request transfers no authority to B** | `authorized_invocation.md` lines 6-8; `relationship=MANDATE`; `delegate_mislabeled_as_mandate` must fail | AIMS §10 distinguishes own-behalf and on-behalf cases. RFC 8693 §1.1 acknowledges that delegation and impersonation do not cover all situations, but `act` and `may_act` specifically assert delegation/acting on behalf | **UNREPRESENTED** | No pinned field names the non-delegating authorized-invocation relation. Encoding it as `act`, `may_act`, or delegated `sub` would change its meaning. |
| **MANDATE: signed relation binds A, B, exact action, audience, request-authority receipt, time, and nonce** | `authorized_invocation.md` lines 17-33; `authority_relation_receipt.schema.json` | AIMS §9.2.1 WPT can hash WIT/access/Txn tokens; §9.2.2 HTTP Message Signatures can cover target and content; Txn-Tokens-09 `tctx` is extensible | **AMBIGUOUS** | The pieces can be packaged, but no pinned profile gives this compound artifact its semantics across protocols. |
| **MANDATE: Gate must jointly verify both live authority paths and the relation before execution** | Mandate conformance Gate cases for requester, executor, and Mandate revocation | AIMS §10.3 requires a resource server to use token identities/claims and allows other authorization systems; §11 requires revocation remediation | **AMBIGUOUS** | A deployment may implement the join, but AIMS does not identify the two-path join or its fail-closed verification algorithm. |
| **MANDATE: Mandate lifetime is contained within both authority windows** | `mandate_exceeds_authority_window` must fail | AIMS credentials/tokens have `nbf`, `iat`, and `exp`; no rule compares a non-delegating instruction window with two independent grants | **AMBIGUOUS** | The timestamps exist, but the containment invariant is not specified. |
| **CROSSING: current presenter/caller is authenticated** | CROSSING-001 TLS run uses verified Keycloak RS256 tokens for `agent-a` and substituted `agent-c` | AIMS §§6-9; RFC 9068 `client_id`; WPT-01 WIT/key proof | **EXACT** | Each hop can identify the workload that actually presents a request. |
| **CROSSING: original A2A requester remains distinct from a credentialed relay/presenter** | CROSSING-001 caller substitution; CROSSING-002 Amendment 2 separates authority path from delivery path | AIMS §9.2 says application authentication can survive intermediaries. RFC 8693 `act` is a delegation chain and prior actors are informational for access control. ID-CHAIN-16 §2.5 permits claim transcription but does not define the representation and permits adding, changing, or removing claims | **UNREPRESENTED** | The pinned profile has no non-delegating `original requester + current presenter` semantic with both identities required at the execution decision. |
| **CROSSING: bind A2A task ID and context ID** | CROSSING-001 `substitute_task_or_context_id`; receipt fields `task_id`, `context_id` | AIMS §11 requires transaction/request correlation; Txn-Tokens-09 `txn`, `rctx`, and `tctx` can carry identifiers | **AMBIGUOUS** | No AIMS/A2A profile says which values are authoritative or requires the MCP-side verifier to compare them. |
| **CROSSING: bind exact MCP tool and canonical arguments** | CROSSING-001 `change_mcp_tool_or_payload`; receipt `tool` and `argument_digest` | AIMS §10.5 and Txn-Tokens-09 §9.2.3 can bind immutable transaction details; AIMS §9.2.2 plus RFC 9421 can sign method, target, and `Content-Digest` for HTTP | **AMBIGUOUS** | Exact HTTP bytes are representable, but the semantic mapping from an A2A task to an MCP tool/arguments tuple is undefined. |
| **CROSSING: bind the intended MCP resource/audience** | Mandate `audience`; different-downstream case in CROSSING-002 | RFC 9068 `aud`; WPT-01 `aud`; RFC 9728 `resource` is required and must match the protected-resource identifier | **EXACT** | AIMS has exact primitives for the target resource and token audience. |
| **CROSSING: bind identity proof to the authorization/action context** | Crossing receipt signs one field set; Gate recomputes against observed crossing | WPT-01 `ath`, `tth`, and `oth` hash related authorization tokens; HTTP Message Signatures cover selected message components | **EXACT for token linkage; AMBIGUOUS for A2A→MCP semantics** | Token substitution can be detected, but correctness still depends on an application profile defining the cross-protocol fields. |
| **CROSSING: freshness** | Receipt `issued_at`, `expires_at`; expired authority mutation | AIMS §§7, 9.2.1, 10.5; RFC 9068 `iat`/`exp`; WPT-01 `exp`; RFC 9421 `created`/`expires` | **EXACT** | Pinned primitives carry and validate bounded lifetimes. |
| **CROSSING: single-use replay refusal across replicas/restarts** | CROSSING-001 replay mutation; transport result shows process-local nonce state fails across requests | WPT-01 `jti`; Txn-Tokens-09 `txn`; RFC 9421 `nonce`. AIMS §9.2.3 says deployments typically use nonce/unique-ID checks. Txn-Tokens-09 §§13.1-13.2 says the token is not replay-resistant and that a workload *can* store `txn`; shared state may be infeasible | **AMBIGUOUS** | Identifiers are exact, but durable atomic consumption is not required. A conforming-looking stateless deployment can replay exactly as Border measured. |
| **CROSSING: upstream authority is current at execution** | CROSSING-001 expired/revoked mutation; Mandate Gate currency checks | AIMS §10.3 token introspection; §11 SSF/CAEP/RISC and MUST enforce received revocation/downgrade signals | **AMBIGUOUS** | AIMS defines propagation/remediation, but not an exact recheck of the original crossing authority at the MCP effect boundary. |
| **CROSSING: non-HTTP/protocol-translated binding** | A2A→MCP crosses two application protocols; receipt uses protocol-neutral stable fields | AIMS §9.2.1 calls WPT protocol-agnostic, but WPT-01 §1 states detailed non-HTTP bindings are future work; HTTP Message Signatures only cover HTTP | **AMBIGUOUS** | The core token is reusable, but no pinned A2A or MCP binding tells independent implementations how to carry and verify the same semantics. |
| **CROSSING: tamper-evident post-hoc reconstruction does not substitute for pre-execution admission** | Receipt states it does not prove action occurrence; Gate verifies before invocation | AIMS §11 requires tamper-evident audit logs and complete-chain reconstruction | **EXACT as audit; UNREPRESENTED as admission evidence** | AIMS exactly requires auditability, but §11 alone cannot supply the pre-execution authority relation needed by either case. |

## Frozen verdict

- `MANDATE != delegation`: **UNREPRESENTED** for the relation; supporting
  identity/token components are exact or ambiguous.
- `A2A -> MCP crossing`: **UNREPRESENTED** for the non-delegating original
  requester versus credentialed presenter distinction; task/action/replay
  enforcement is ambiguous.
- Overall: **AIMS-FIT-001 = RED_WITH_AMBER_COMPONENTS**.

Green remains strategically useful: a future AIMS revision or named companion
profile can turn any row exact without changing Border's cases.
